from typing import List


class Solution:
    def minOperations(self, nums: List[int], x: int) -> int:
        def _recur(arr, idx, result):
            if not arr:
                pass
            elif len(arr) == 1:
                pass
            elif result == 0:
                pass

        if not nums:
            return -1
